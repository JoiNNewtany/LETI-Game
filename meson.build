project('LetiGame', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++20'])

exe = executable('letigame', 'src/main.cpp',
  install : true)

test('basic', exe)
